---
name: uranai-workers
description: オリジナル占いアプリを対話形式で作成し、Cloudflare Workers にデプロイできるコードを生成するスキル。index.html ではなく src/index.js + wrangler.toml の構成で出力する本格 Web アプリ版。ユーザーが「Workersで占いを作りたい」「Workersで診断アプリを作って」「本格的な占いアプリを作りたい」「/uranai-workers」と言ったら必ずこのスキルを起動すること。
---

# uranai-workers — オリジナル占いアプリ ジェネレーター（Cloudflare Workers版）

ユーザーへの質問に答えてもらうだけで、オリジナルの占いアプリを生成する。
生成物は **Cloudflare Workers** にデプロイできる構成（`src/index.js` + `wrangler.toml`）で出力する。
`npx wrangler deploy` の1コマンドで本物の Web アプリとして世界に公開できる。

---

## ステップ0: 初期化（スキル起動時に必ず最初に実行）

**インタビューを始める前に、必ずこのステップを完了させること。**
中断後に再開する場合も、途中から再開せず必ずステップ0からやり直す。

### 処理1: 作業フォルダの確認
Bash ツールで `pwd` を実行し、現在の作業フォルダのフルパスを取得・記憶する。

### 処理2: ファイル衝突チェック
Bash ツールで以下を実行し、既存ファイルがあるか確認する：
```bash
ls wrangler.toml src/index.js 2>/dev/null && echo "EXISTS" || echo "CLEAN"
```
- **CLEAN（ファイルなし）** → **通常モード**：`{pwd}` 直下に出力する
- **EXISTS（ファイルあり）** → **サブフォルダモード**：`{pwd}/{プロジェクト名}/` に出力する（プロジェクト名はインタビューQ9で決定）

### 処理3: 状態リセット
以下のインタビュー状態チェックリストを**すべて未回答にリセット**する：
```
[ ] Q1: テーマ
[ ] Q2: キャラクター数
[ ] Q3: キャラクター名
[ ] Q4: キャラクターの説明
[ ] Q5: 判定方法
[ ] Q6: デザインの雰囲気
[ ] Q7: メインカラー
[ ] Q8: タイトル
[ ] Q9: プロジェクト名
```

### 処理4: 開始メッセージ
以下を表示してからQ1を聞く：
```
🎉 占いアプリ作成を開始します！
いくつか質問に答えてください。1問ずつ聞いていきます。
```

---

## ステップ1: インタビュー

### ⚠️ インタビュー進行の絶対ルール

- **1問ずつ順番に**聞く。複数を同時に聞くことは**絶対禁止**。
- 必ず**ユーザーの回答が届いてから**次の質問を送る。
- 上のチェックリストを頭の中で管理し、✅ になってから次へ進む。
- **Q1から順番に必ず進める。** Q2を聞く前にQ1の回答が必要。Q3を聞く前にQ2の回答が必要。
- 会話が途中で途切れてスキルを再起動した場合は、**ステップ0に戻ってリセット**する。途中から再開しない。

### 質問リスト

1. **テーマ**（Q1回答受信後→Q2へ）
   「どんな占いにしますか？（例：昆虫占い、お魚占い、家電占い、なんでもOK）」

2. **キャラクター数**（Q2回答受信後→Q3へ）
   「何種類のキャラクターに分けますか？（例：6・8・12など）」

3. **キャラクター名**（Q3回答受信後→Q4へ）
   「キャラクターの名前を教えてください。{数}種類分。
   （思いつかない場合は『考えて下さい』と言ってもらえれば提案します）」
   提案した場合は最後に「変えたいものがあれば言ってください。変更がなければOKと言って下さい」と添える。

4. **各キャラクターの説明**（Q4回答受信後→Q5へ）
   「各キャラクターの性格・特徴を一言で教えてください。
   （思いつかない場合は『考えて下さい』と言ってもらえれば提案します）」
   提案した場合は最後に「変えたいものがあれば言ってください。変更がなければOKと言って下さい」と添える。

5. **判定方法**（Q5回答受信後→Q6へ）
   「判定方法はどうしますか？
   　A) 誕生日（生年月日）を入力して判定
   　B) 質問に答えて判定（性格診断タイプ）
   　C) 両方使う」

   - **A または C を選んだ場合**: 誕生日から判定するアルゴリズムを自動設計する（ユーザーへの説明は不要）
   - **B または C を選んだ場合**: 「どんな質問をしますか？いくつくらい？（例：3〜5問）」と聞く。質問内容は提案してもよい。この追加質問への回答も受け取ってからQ6へ進む。

6. **デザインの雰囲気**（Q6回答受信後→Q7へ）
   「デザインはどんな雰囲気にしますか？
   　例：かわいい系・クール系・ポップ系・和風・シンプル など」

7. **カラー**（Q7回答受信後→Q8へ）
   「メインカラーはありますか？（例：ピンク、青、緑など。なければ『おまかせ』でOK）」

8. **タイトル**（Q8回答受信後→Q9へ）
   「占いアプリのタイトルを教えてください。（例：『あなたはどの昆虫？』）」

9. **プロジェクト名（英数字・ハイフンのみ）**（Q9回答受信後→ステップ2へ）
   今日の日付から初期値を自動生成して提案する。
   - 初期値: `libe{MMDD}` （例：6月6日なら `libe0606`）
   - 「プロジェクト名は `libe0606` で良いですか？変えたい場合は言ってください。」と聞く。
   - ユーザーが「OK」「それで」など肯定したらそのまま進む。
   - 変更したい場合は英数字とハイフンのみ使えることを伝えて入力してもらう。
   ※ これが公開URLの一部になります（例：`libe0606.あなたのID.workers.dev`）

---

## ステップ2: 確認

インタビューが終わったら、収集した情報を箇条書きで整理してユーザーに見せる。
「この内容でアプリを作ります。よろしいですか？」と確認してから生成に進む。

---

## ステップ3: アプリ生成

### ⚠️ ファイルの書き出し（最重要）

コードをチャットに表示するだけでは不十分。
**必ず以下の手順でファイルをディスクに書き出すこと。**

#### 手順1: 出力先フォルダの決定

ステップ0の衝突チェック結果に応じて出力先を決める：

**通常モード（CLEAN）の場合：**
- 出力先 = `{ステップ0で取得したpwdのフルパス}`
- 例：`/Users/username/MyClaudeCode/libe0606`

**サブフォルダモード（EXISTS）の場合：**
- 出力先 = `{ステップ0で取得したpwdのフルパス}/{プロジェクト名}`
- まず Bash ツールで `mkdir -p {出力先}/src` を実行してフォルダを作成する
- 例：`/Users/username/MyClaudeCode/libe0606/libe0607`

#### 手順2: ファイルを作成する
Write ツールで以下の2ファイルを作成する：

1. `{出力先}/wrangler.toml`
2. `{出力先}/src/index.js`

#### 手順3: ユーザーにフルパスを伝える
「以下のフォルダにファイルを作成しました」と出力先フルパスを明示する。

### 出力フォルダ構成

**通常モード（1本目）:**
```
{作業フォルダ}/
├── src/
│   └── index.js
└── wrangler.toml
```

**サブフォルダモード（2本目以降）:**
```
{作業フォルダ}/
└── {プロジェクト名}/
    ├── src/
    │   └── index.js
    └── wrangler.toml
```

---

### wrangler.toml の内容

```toml
name = "{プロジェクト名}"
main = "src/index.js"
compatibility_date = "2024-01-01"
preview_urls = false
```

**`preview_urls = false` は必ず設定すること**（省略禁止）。
これを省略すると認証をバイパスされるセキュリティリスクがある。

---

### src/index.js の構造

HTML コンテンツ全体をテンプレートリテラルで `const HTML` に格納し、
Workers の fetch ハンドラーでそのまま返す。

```javascript
const HTML = `<!DOCTYPE html>
<html lang="ja">
<head>
  ...（HTMLヘッダー）
</head>
<body>
  ...（HTML本文）
<script>
  ...（JavaScriptコード）
</script>
</body>
</html>`;

export default {
  async fetch(request, env, ctx) {
    return new Response(HTML, {
      headers: { 'Content-Type': 'text/html; charset=UTF-8' },
    });
  },
};
```

---

### ⚠️ エスケープの絶対ルール（最重要）

`src/index.js` の外側はテンプレートリテラル（バッククォート）で囲まれている。
HTML の中の JavaScript でもテンプレートリテラルを使う場合、**必ずエスケープする**。

| 書きたいもの | src/index.js での書き方 |
|---|---|
| バッククォート `` ` `` | `` \` `` |
| `${変数}` | `\${変数}` |
| 通常の文字列 `'...'` `"..."` | そのまま（エスケープ不要） |

**【推奨】エスケープ事故を防ぐ書き方:**
HTML 内の JavaScript では、テンプレートリテラルの代わりに **文字列連結（`+`）** を使うと
エスケープが一切不要になり、事故を防げる。

```javascript
// テンプレートリテラルを使わず、文字列連結で書く（エスケープ不要・安全）
year.insertAdjacentHTML('beforeend', '<option value="' + y + '">' + y + '年</option>');
```

---

### HTML/CSS/JS の要件

- **1ファイル完結**（外部ライブラリ・CDN 不使用）
- スマートフォンで見やすいレスポンシブデザイン（`max-width: 480px` 推奨）
- 文字コードは UTF-8

---

### 画面構成（必須）

**1. タイトル画面**
- タイトルとキャッチコピー
- テーマに合った大きな絵文字（`@keyframes float` で浮遊アニメーション）
- 「診断スタート」ボタン

**2. 生年月日入力画面**（判定方法 A または C の場合）
- 年・月・日のセレクトボックス
- 「次へ →」ボタン

**3. 質問画面**（判定方法 B または C の場合）
- 質問を1問ずつ表示
- 進捗インジケーター（例：`QUESTION 1 / 3`）
- 選択肢ボタン（クリックで自動的に次へ）

**4. 結果画面**
- キャラクター絵文字（大きく、`@keyframes pop` でポップアニメーション）
- `YOUR {テーマ}` タグ
- キャラクター名（大きく）
- キャラクターの説明文
- ラッキーカラー・ラッキーナンバー（2列）
- 相性のいいキャラクター
- 仕事・恋愛・お金の運勢（各1行）
- 今月の運勢（現在月で3パターン切替: `new Date().getMonth() % 3`）
- 「もう一度診断する」ボタン

---

### 判定アルゴリズム

**誕生日スコア（A または C の場合）:**
```javascript
birthdayScore = ((年の下2桁) + 月 + 日) % キャラクター数
```

**質問スコア（B または C の場合）:**
各選択肢にタイプ別スコアを割り当て（例：`[3, 0, 0, 0]`）、合計点でタイプを決定。
同点の場合はランダムに1つ選ぶ。

**最終判定（C の場合）:**
タイプ内のキャラクターを `birthdayScore % グループ数` で絞り込む。

---

### キャラクターデータの構成（充実版）

```javascript
const CHARACTERS = [
  {
    name: 'キャラクター名',
    emoji: '🦋',
    desc: '短い説明文（2文以内）',
    luckyColor: '🔴 赤',
    luckyNumber: 7,
    compatible: ['キャラA', 'キャラB'],
    work: '仕事の運勢（15文字以内）',
    love: '恋愛の運勢（15文字以内）',
    money: 'お金の運勢（15文字以内）',
    monthly: ['今月の一言A', '今月の一言B', '今月の一言C'],
  },
  // ... 全キャラクター分
];
```

---

## ステップ4: 完成後のメッセージ

アプリ生成後、以下を伝える：

```
✅ 占いアプリが完成しました！（Cloudflare Workers版）

📁 作成場所:
{出力先フルパス}/
├── src/index.js
└── wrangler.toml

【デプロイ手順】

① 作成フォルダに移動
   cd {出力先フルパス}

② Cloudflare にログイン（初回のみ）
   npx wrangler login

③ デプロイ！
   npx wrangler deploy

④ 表示された URL にアクセスして確認
   https://{プロジェクト名}.{アカウント名}.workers.dev

🎉 これで本物の Web アプリが世界に公開されました！

【ローカルで動作確認する場合】
   npx wrangler dev
   → ブラウザで http://localhost:8787 を開く

【GitHub で管理する場合】
   git init

   .gitignore ファイルを以下の内容で作成すること：
   ---
   .wrangler/
   node_modules/
   .env
   .env.local
   .DS_Store
   ---

   git add .
   git commit -m "first commit"
   gh repo create {プロジェクト名} --private --source=. --remote=origin --push
```
